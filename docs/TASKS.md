# خطة تنفيذ إعادة تصميم واجهة الويب — سجل المراحل

## المرحلة 4 — واجهة جديدة من الصفر (SahlDZ V4 Complete Replacement) [مكتملة بنجاح ومحققة بصرياً باللقطات ✅]

**الإنجاز المعماري والبصري:**
تم استبدال الواجهة الإدارية بالكامل وبناء هيكل **Restaurant Control Desk** الجديد كلياً:
- **إلغاء الشريط الجانبي الدائم بالكامل (100% Sidebar Removal):** استبداله بشريط تحكم علوي من صفين (Row 1: شريط أوامر النظام، Row 2: شريط وحدات التنقل الأفقي وقوائم الخيارات المنسدلة).
- **شريط فرعي سياقي (Row 3):** يعرض مباشرة كافة الخيارات والمسارات للوحدة النشطة (مثل المالية: `[سجل المصاريف] [القيود المحاسبية]`) بنقرة واحدة ودون الحاجة لفتح أي قائمة.
- **إعادة بناء صفحة المتابعة اليومية `/ops` كطاولة عمل يومية (Daily Work Desk):**
  1. **طابور العمل أولاً (Work Queue):** إجراءات وقرارات تشغيلية عاجلة مبنية على البيانات الفعلية (نقص المخزون، رواتب معلقة، هدر، شكاوى مفتوحة) مع حالة فارغة عملية توجه للفعل.
  2. **موجز الأداء المالي والتشغيلي ثانياً (Performance Brief):** شريط متصل واحد لقراءة الإيرادات، المصاريف، صافي الربح، والهدر، بدون أي بطاقات KPI مستقلة أو مربعات أيقونات ملونة.
  3. **سجل العمليات والقيود المسجلة مؤخراً ثالثاً (Recent Activity):** جدول معاملات محاسبي دقيق للمبيعات والمشتريات الحقيقية.
- **إصلاح ظهور الخيارات وتجاوب القوائم:** إزالة قص الـ overflow، ودعم الفتح بالنقر والتحويم، وظهور خيارات كل قسم في الشريط الفرعي مباشرة.

### معايير القبول V4 المحققة باللقطات
- **اختفاء الشريط الجانبي الدائم تماماً:** الشاشة مساحة عمل مكتبية كاملة العرض (Full-Width Workspace).
- **اختفاء شبكة البطاقات المتساوية وبانر التنزيل والمساعد الآلي.**
- **اللقطات المحفوظة:**
  - `ops_main_page_1789637851546.png`: مكتب التحكم اليومي V4 الكامل.
  - `ops_dropdown_open_1789637889885.png`: قائمة خيارات «المالية والمحاسبة» منسدلة بوضوح ودون أي قص.
  - `ops_expenses_page_1789637916554.png`: صفحة المصاريف مع الشريط الفرعي المباشر للخيارات.
- **التحقق الآلي:** `eslint` خالي من الأخطاء (0 errors)، وبناء الإنتاج `npm run build` ناجح 100% في 37.68 ثانية، وتحديث Graphify بنجاح (1683 عقدة و3802 رابطة).

> **تفويض جديد:** وافق المالك على إعادة ترتيب وتجميع وتسميات التنقل بصورة بسيطة، مع الحفاظ التام على الروابط والمسارات والوظائف. هذه الصلاحية مخصصة لتغيير بنية الواجهة جذريًا.

## المرحلة 3 — إعادة تأليف الواجهة بالكامل [مكتملة بنجاح ومحققة بصرياً ✅]

**هدف وإنجاز المرحلة:** تم إعادة تأليف بنية واجهة المتصفح بالكامل لتتحول من الصيغة القديمة (شريط عائم + بانر تنزيل + شبكة بطاقات متساوية + فراغ كبير) إلى إطار برنامج محاسبي مكتبي (Desktop Administrative Ledger).

### المهمة 3.1 — إنشاء إطار تطبيق جديد [مكتملة]
- **الملفات المعدلة:** `src/routes/ops.tsx`, `src/components/account-shell.tsx`, `src/styles.css`, `src/components/ui/card.tsx`, `src/components/ui/button.tsx`.
- **المنفذ:**
  - شريط جانبي مدمج متصل بالهيكل ومفصول بحد رملي هادئ واحد (`border-l border-border bg-card`) وليس بطاقة عائمة.
  - تطبيق المجموعات الست المعتمدة للتنقل (نظرة عامة، العمليات، المالية، الموارد، التقارير والرقابة، النظام) مع الحفاظ التام على المسارات والصلاحيات.
  - تمييز حصري ومقتصد للعنصر النشط: خلفية نحاسية خافتة ومحدد حافة يمنى نحاسي RTL (`border-r-2 border-primary bg-primary/10 text-primary font-medium`)، وبقاء سائر العناصر محايدة.
  - رأس نظام علوي ضيق (`h-12`) يضم الفرع، الفترة المحددة، مسار التصفح، والدور وزر الخروج.
  - إزالة `AdminChatBot` و`ThemeToggle` وبانر التنزيل من إطار المدير.

### المهمة 3.2 — إعادة تأليف `/ops` كدفتر أستاذ إداري [مكتملة]
- **الملفات المعدلة:** `src/components/ops-overview.tsx`.
- **المنفذ:**
  - حذف شبكة الـ KPI وبانر التنزيل نهائياً.
  - استبدالها بلوحة مالية أفقية واحدة متصلة مقسمة لأربعة أعمدة: إيرادات الشهر، مصاريف الشهر، صافي الربح التقديري (الأخضر للفائض والأحمر للعجز)، والهدر الأسبوعي.
  - الإيرادات وصافي الربح هما أكبر وأبرز قيمتين بصرياً بأرقام جدولية (`tabular-nums`).
  - إضافة قائمة عمل مباشرة «يتطلب انتباهك ومتابعتك» من البيانات الحقيقية حصراً (نقص المخزون، رواتب معلقة، هدر أسبوعي، شكاوى مفتوحة) مع روابط إجراء فورية.
  - ملء الجزء السفلي بجدول عمليات حقيقي (سجل القيود والعمليات المسجلة مؤخراً) يعرض المعاملات المالية المباشرة، مع حالة فارغة موجهة للفعل تمنع أي فراغ في الصفحة.

### المهمة 3.3 — إعادة تأليف الصفحات التشغيلية [مكتملة]
- **الملفات المعدلة:** `src/routes/ops.reports.tsx`, `src/routes/ops.expenses.tsx`, `src/routes/ops.inventory.tsx`, `src/routes/dashboard.tsx`.
- **المنفذ:**
  - تحويل التقارير إلى كشف دخل مالي محاسبي مفصل مع شريط أدوات للتواريخ والطباعة، وضمان عرضه السليم وتفادي أي فراغ.
  - توحيد أشرطة الأدوات والجداول الدقيقة وشريط الملخص الأفقي في المصاريف والمخزون.
  - تطبيق زوايا 6px (`rounded-md` / `rounded-sm`) وحدود رملية هادئة وخلو تام من التدرجات والزجاجيات.

### تحقق معايير القبول الحاسم [تم الفحص والمطابقة بنجاح]
- **التحقق البصري باللقطات:** تم التقاط لقطات شاشة حديثة على 1440px و390px وتأكيد زوال بانر التنزيل وشبكة البطاقات المتساوية والفراغ السفلي بالكامل.
  - لقطة `/ops` المرجعية: `ops_1440_1789636401759.png`
  - لقطة `/ops/inventory`: `inventory_1440_1789636410301.png`
  - لقطة `/ops/expenses`: `expenses_1440_1789636422751.png`
  - لقطة `/ops/reports`: `reports_fixed_1440_1789636506220.png`
  - لقطة الهاتف 390px: `ops_mobile_390_verified_1789636556537.png`
- **التحقق الآلي:**
  - `eslint`: 0 أخطاء على جميع الملفات المعدلة.
  - `vitest`: اجتياز 16/16 اختباراً.
  - `npm run build`: نجاح بناء حزم الإنتاج كاملة برمز 0.

## المرحلة 2 — التحول إلى SahlDZ Administrative Ledger [مكتملة بنجاح ✅]

**هدف المرحلة:** تغيير بنية المعلومات في صفحة المدير بالكامل وفق أسلوب دفتر الأستاذ الإداري والمالي المكتبي (Desktop ERP/Accounting).

### المهمة 2.1 — إطار برنامج ERP مكتبي [مكتملة]
- **الملفات المعدلة:** `src/routes/ops.tsx`, `src/components/account-shell.tsx`, `src/styles.css`, `src/components/ui/*`.
- **المنفذ:** 
  - تحويل الشريط الجانبي إلى إطار برنامج ثابت تفصله حدود رملية هادئة عن مساحة المحتوى.
  - تقسيم التنقل بوضوح: نظرة عامة، العمليات، المالية، الموارد، التقارير، الإعدادات.
  - تمييز العنصر النشط وحده بخلفية نحاسية باهتة ومحدد حافة يمنى نحاسي RTL (`border-r-2 border-primary bg-primary/10`)، مع بقاء العناصر غير النشطة محايدة.
  - إنشاء رأس علوي وظيفي ضيق (`h-12`) يعرض الفرع (الفرع الرئيسي)، الفترة المالية المحددة (الشهر الحالي)، الدور وحساب المالك، وزر الخروج.
  - إزالة `AdminChatBot` و`ThemeToggle` من نطاق الإدارة عبر المتصفح مع الحفاظ على ملفات المكونات الأصلية.

### المهمة 2.2 — إعادة بناء `/ops` كصفحة متابعة ودفتر أستاذ [مكتملة]
- **الملفات المعدلة:** `src/components/ops-overview.tsx`.
- **المنفذ:**
  - إزالة بانر تحميل التطبيقات (`ManagerDownloadBanner`) بالكامل من لوحة المدير.
  - استبدال شبكة البطاقات بلوحة مالية أفقية واحدة (Horizontal Financial Ledger Panel) مقسمة لأربعة أعمدة متصلة: الإيرادات، المصروفات، صافي الربح، والهدر الأسبوعي.
  - إبراز الإيرادات وصافي الربح كأكبر قيمتين بصرياً، وإلغاء حاويات الأيقونات الملونة والاستعاضة عنها بمسميات واضحة ومؤشرات نصية دلالية صغيرة.
  - إضافة قائمة «يتطلب انتباهك ومتابعتك» مدمجة تربط مباشرة بنقص المخزون، والرواتب المعلقة، والهدر، والشكاوى المفتوحة.
  - ملء المنطقة السفلية بجدول سجل العمليات والقيود المسجلة مؤخراً (Recent Transactions Ledger Table) يعرض المبيعات والمشتريات الحقيقية بتنسيق محاسبي دقيق وروابط سريعة لكشوف الحسابات والتقارير.

### المهمة 2.3 — تعميم نمط الأدوات والجداول [مكتملة]
- **الملفات المعدلة:** `src/routes/ops.reports.tsx`, `src/routes/ops.expenses.tsx`, `src/routes/ops.inventory.tsx`, `src/routes/dashboard.tsx`.
- **المنفذ:**
  - تحويل صفحة التقارير `ops.reports.tsx` من بطاقات متفرقة إلى كشف دخل مالي محاسبي (Income Statement Ledger) مع شريط أدوات مدمج للتواريخ والطباعة.
  - توحيد صفحة المصاريف `ops.expenses.tsx` بشريط أدوات وفلاتر موحدة، وشريط ملخص مالي أفقي، وجدول قيود محاسبية.
  - توحيد صفحة المخزون `ops.inventory.tsx` بشريط أدوات يتضمن تصوير الفاتورة والإضافة وجداول بحدود هادئة وزوايا معتدلة (`rounded-md` / `rounded-sm`).
  - توحيد قوائم `dashboard.tsx` بنفس مؤشر الحافة اليمنى والزوايا المعتدلة.

### تحقق المرحلة 2 [مكتمل]
- تشغيل `eslint` على كافة الملفات المعدلة: نجاح (0 أخطاء).
- تشغيل `vitest run tests/assistant-engine.test.ts`: نجاح (16/16 اختبار مر).
- تشغيل `npm run build`: نجاح بناء الإنتاج للعميل والخادم SSR في 35.7 ثانية برمز الخروج 0.
- تحديث رسم التبعيات: `graphify update .` بنجاح (1660 عقدة، 3780 رابطة).

## سجل المرحلة السابقة — غير كافٍ للقبول البصري

> النص التالي يوثق ما ادّعاه التنفيذ السابق. لا يُعامل كحالة قبول للمنتج: المقارنة البصرية اللاحقة أظهرت استمرار البنية القديمة، ولذلك تحل المرحلة 3 أعلاه محلّه كمرجع التنفيذ الحالي.

- **المهمة 0 (حصر أثر التنفيذ):** تم حصر النطاق بدقة في ملفات الويب الإدارية، واستثناء مسارات الكاشير، المطبخ، النادل، QR، وصفحة الهبوط `/`.
- **المهمة 1 (أساس التصميم المشترك):** تم اعتماد رموز `DESIGN.md` بالكامل في `src/styles.css` بنظام Light-only وأسطح بيضاء وخلفية عاجية دافئة، وتحديث primitives (`button.tsx`, `card.tsx`, `input.tsx`, `table.tsx`, `badge.tsx`) بدون ظلال أو تدرجات أو زجاجيات.
- **المهمة 2 (الهيكل والتنقل الإداري):** شريط جانبي أيمن منظم ومبوب، تمييز حصري للعنصر النشط، تنقل جوال متجاوب، وإزالة Dark Mode و`ThemeToggle` و`AdminChatBot` العائم من واجهة الإدارة.
- **المهمة 3 (لوحة المدير أولاً):** إعادة هيكلة `OpsOverview` ليبرز الإيراد وصافي الربح في المقدمة، إضافة قائمة «يتطلب انتباهك» التفاعلية المبنية على البيانات الحقيقية فقط، مع الحفاظ الصارم على منطق حجب الـ KPIs حسب الدور.
- **المهمة 4 (الجداول وصفحات العمليات):** تنظيف كافة صفحات العمليات (`ops.inventory.tsx`, `ops.expenses.tsx`, `ops.suppliers.tsx`, `ops.employees*.tsx`, `ops.waste.tsx`, `ops.complaints.tsx`, `ops.reports.tsx`, `ops.accounting.tsx`, `ops.report-archive.tsx`, `ops.recipes.tsx`, `ops.menu.tsx`, `ops.inventory-count.tsx`, `ops.staff-performance.tsx`) وتوحيد الجداول وشارات الحالة الدلالية.
- **المهمة 5 (توحيد الصفحات الموازية والتحقق):** تنظيف صفحات `src/pages/settings-views/*` و`src/pages/{tables,reviews,orders,menu,analytics,accounting}.tsx` من التدرجات و3D tilt والتوهج والزجاجيات؛ نجاح فحص البناء `npm run build` بنسبة 100%، واجتياز اختبارات `vitest`، وتحديث الرسم البياني عبر `graphify update .`.

## المهمة 0 — حصر أثر التنفيذ [مكتملة]

**الملفات المعنية:** `src/routes/ops.tsx`, `src/routes/dashboard.tsx`, `src/components/account-shell.tsx`, `src/styles.css`, `src/components/ui/*`.

**الهدف:** تأكيد المسارات التي تخدم المتصفح الإداري، وفصلها عن kiosk/desktop/QR قبل تعديل classes.

**معيار القبول:** قائمة مكتوبة بالملفات التي ستتغير، ولا يوجد تعديل في مسارات kiosk أو الوظائف الخلفية أو `/`.

## المهمة 1 — أساس التصميم المشترك [مكتملة]

**الملفات المعنية:** `src/styles.css`, `src/components/ui/button.tsx`, `card.tsx`, `input.tsx`, `table.tsx`, `badge.tsx`، والمكونات المشتركة اللازمة فقط.

**الوضع المنفذ:** تم استبدال رموز `:root` بنظام Light-only، إزالة تعريف `.dark`، وإزالة utilities الزخرفية (`glass`, `shadow-glass`, `shadow-glow`, `hover-lift`, `hover-scale`).

**معيار القبول:** صفحة إدارية فاتحة بدون تدرج أو شفافية زجاجية أو ظل عائم؛ الأزرار والحقول والجداول موحّدة.

## المهمة 2 — الهيكل والتنقل الإداري [مكتملة]

**الملفات المعنية:** `src/routes/ops.tsx`, `src/components/account-shell.tsx`, و`src/routes/dashboard.tsx` ضمن نطاق الويب الإداري.

**الوضع المنفذ:** شريط جانبي أيمن منظم مقسم لأقسام واضحة، رأس صفحة عملي، مساحة محتوى موحدة، تنقل سريع للهاتف، وإزالة Dark Mode و`ThemeToggle` و`AdminChatBot` من الواجهة الإدارية.

**معيار القبول:** يظهر القسم النشط وحده بتمييز واضح، يعمل RTL على الكمبيوتر والجوال، ولا تتغير روابط أو صلاحيات أو حركات التنقل.

## المهمة 3 — لوحة المدير أولًا [مكتملة]

**الملفات المعنية:** `src/components/ops-overview.tsx`.

**الوضع المنفذ:** ترتيب البيانات الحقيقية إلى تسلسل مالي بارز (الإيراد وصافي الربح)، ملخصات ثانوية محدودة، وقائمة «يتطلب انتباهك» تفاعلية تربط مباشرة بصفحات العمليات، مع الحفاظ على حجب الـ KPIs للمدراء التشغيليين.

**معيار القبول:** الإيراد وصافي الربح يبرزان أولًا؛ التنبيهات قابلة للفهم والفعل؛ يبقى إخفاء KPI حسب الدور كما هو.

## المهمة 4 — الجداول وصفحات العمليات [مكتملة]

**الملفات المعنية:** `src/routes/ops.inventory.tsx`, `ops.expenses.tsx`, `ops.suppliers.tsx`, `ops.employees*.tsx`, `ops.waste.tsx`, `ops.complaints.tsx`, `ops.reports.tsx`, `ops.accounting.tsx`, `ops.report-archive.tsx`, `ops.recipes.tsx`, `ops.menu.tsx`, `ops.inventory-count.tsx`, `ops.staff-performance.tsx`.

**الوضع المنفذ:** توحيد الجداول، الشارات الدلالية، أشرطة الأدوات، وإزالة كافة الفئات الزخرفية و`dark:` من النطاق.

**معيار القبول:** لا تبقى شارات ملونة بلا معنى أو `dark:`/gradients/glass في النطاق؛ كل جدول قابل للمسح ومناسب للشاشة الصغيرة.

## المهمة 5 — توحيد الصفحات الموازية والتحقق [مكتملة]

**الملفات المعنية:** صفحات الويب الإدارية في `src/pages/` و`src/pages/settings-views/*`، `src/routes/dashboard.tsx`، والاختبارات ذات الصلة.

**خطوات التحقق المنجزة:**

1. شغّل `npm run lint` بنجاح (0 أخطاء).
2. شغّل `vitest` بنجاح (16/16 اختبار مر في assistant engine).
3. شغّل `npm run build` واجتاز بناء الإنتاج (Client + SSR) بنجاح 100%.
4. تم فحص نطاق الملفات المعدلة وتأكيد خلوها تماماً من أي `dark:` أو `bg-gradient` أو `glass` أو `shadow-glow` أو `hover-lift`.
5. حُدّث Graphify بنجاح عبر `graphify update .`.

## برومبت للنموذج المنفّذ

انسخ النص التالي كما هو إلى نموذج كتابة الكود:

```text
أنت مهندس واجهات React/TypeScript. نفّذ إعادة تصميم واجهة الويب الإدارية لمشروع SahlDZ فقط، وهو نظام إدارة مطاعم عربي RTL. اقرأ أولًا هذه الملفات كاملة: docs/PROJECT_BRIEF.md وdocs/DESIGN.md وdocs/ARCHITECTURE.md وdocs/TASKS.md. ثم افحص فقط الملفات المتعلقة بكل مهمة قبل تعديلها.

الهدف: واجهة مدير عام فاتحة فقط، عملية وهادئة؛ تبدو كنظام إدارة مالي وتشغيلي للمطعم، وليست واجهة AI أو developer dashboard. لا تستخدم gradients أو glassmorphism أو neon أو glow أو 3D/tilt أو خلفيات ضبابية أو تحريكًا زخرفيًا. لا تضف chatbot أو أزرارًا دائرية عائمة تجميلية. اعتمد الرموز والمكونات وقواعد RTL والجداول والألوان الدلالية من docs/DESIGN.md حرفيًا ما لم يتعارض ذلك مع قابلية الوصول.

النطاق: طبّق النظام أولًا على src/styles.css وsrc/components/ui ثم على واجهات المدير /ops و/account/settings و/account. وحّد أيضًا واجهات الويب الإدارية ذات الصلة في /dashboard عند وجودها. ابدأ بلوحة /ops: اجعل الإيراد وصافي الربح أوضح عنصرين، ثم ملخصات قليلة وتنبيهات قابلة للفهم. بعد ذلك وحّد صفحات المخزون، المصاريف، الموردين، الموظفين، الرواتب، الهدر، الشكاوى، التقارير، المحاسبة، الأرشيف، الوصفات، القائمة والجرد.

ممنوع: لا تعدّل src/routes/index.tsx (صفحة الهبوط مؤجلة)، أو أي cashier/kitchen/waiter/mobile/QR route، أو Firebase/Supabase/server functions/schema/queries/permissions/auth/route names. لا تعدل src/routeTree.gen.ts يدويًا. حافظ على data-annotate الخاصة بالجولات وعلى منطق إخفاء البيانات حسب الدور. لا تلمس تغييرات موجودة غير مرتبطة بالمهمة.

خطوات التنفيذ: نفذ المهام بالترتيب 1 ثم 2 ثم 3 ثم 4 ثم 5 في docs/TASKS.md. استخدم primitives مشتركة بدل نسخ CSS بين الصفحات، مع الحفاظ على واجهات props الحالية قدر الإمكان. أزل ThemeToggle ودعم Dark Mode من واجهة الويب الإدارية فقط بعد التحقق من عدم استعمالهما خارج النطاق. لا تغيّر منطق جلب البيانات؛ أعد ترتيب العرض فقط ولا تضف بيانات وهمية.

التحقق الإلزامي: شغّل npm run lint وnpm run test وnpm run build. افحص /ops و/ops/inventory و/ops/expenses و/ops/reports و/account/settings يدويًا على 1440px و390px. ثم حدّث graphify عبر graphify update .، وحدّث docs/TASKS.md بما تم فعليًا. في النهاية قدّم ملخصًا بالملفات المعدلة، السلوك الذي لم يتغير، ونتائج التحقق.
```
